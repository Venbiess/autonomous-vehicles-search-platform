version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: "avsp-postgres-${USER}"
    environment:
      POSTGRES_DB: avsp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    networks:
      - avsp-net

  minio:
    image: minio/minio:latest
    container_name: "minio-${USER}"
    command: server /data --console-address ":1001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-data:/data
    ports:
      - "1000:1000"
      - "1001:1001"
    networks:
      - avsp-net

  embedder:
    build:
      context: ../..
      dockerfile: docker/models/models-cpu.Dockerfile
    container_name: "embedder-${USER}"
    volumes:
      - "${PWD}/../..:/app"
    ports:
      - "8000:8000"
    networks:
      - avsp-net

  avsp-server:
    build:
      context: ../..
      dockerfile: docker/server/server.Dockerfile
    image: avsp-server
    container_name: "avsp-server-${USER}"
    depends_on:
      - postgres
      - minio
      - embedder
    volumes:
      - "${PWD}/../..:/app"
      - "vscode-server:/root/.vscode-server"
    ports:
      - "1002:1000"
    environment:
      MINIO_ENDPOINT: http://minio:1000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: waymo-processed
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: avsp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - avsp-net

volumes:
  minio-data:
  postgres-data:
  vscode-server:

networks:
  avsp-net:
    driver: bridge
