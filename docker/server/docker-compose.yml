services:
  clickhouse:
    image: clickhouse/clickhouse-server:24.8
    container_name: "clickhouse-${USER}"
    environment:
      CLICKHOUSE_DB: default
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: ""
    volumes:
      - clickhouse-data:/var/lib/clickhouse
    ports:
      - "8123:8123"
      - "9000:9000"
    networks:
      - avsp-net

  postgres:
    image: pgvector/pgvector:pg16
    container_name: "avsp-postgres-${USER}"
    environment:
      POSTGRES_DB: avsp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    networks:
      - avsp-net

  minio:
    image: minio/minio:latest
    container_name: "minio-${USER}"
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    networks:
      - avsp-net

  minio-init:
    image: minio/mc:latest
    container_name: "minio-init-${USER}"
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c '
      until mc alias set local http://minio:9000 minioadmin minioadmin; do
        sleep 2;
      done &&
      for bucket in waymo argoverse avsp; do
        mc mb --ignore-existing local/$$bucket;
        mc anonymous set download local/$$bucket;
      done
      '
    networks:
      - avsp-net

  embedder:
    build:
      context: ../..
      dockerfile: docker/models/models-cpu.Dockerfile
    container_name: "embedder-${USER}"
    volumes:
      - "${PWD}/../..:/app"
    ports:
      - "8000:8000"
    networks:
      - avsp-net

  vlm:
    build:
      context: ../..
      dockerfile: docker/models/models-cpu.Dockerfile
    container_name: "vlm-${USER}"
    volumes:
      - "${PWD}/../..:/app"
    command: >
      /bin/sh -c "
      uvicorn backend.models.vlm.vlm:app
      --host 0.0.0.0
      --port 8001
      --log-level debug
      --reload
      "
    ports:
      - "8001:8001"
    networks:
      - avsp-net

  analytics-server:
    build:
      context: ../..
      dockerfile: docker/storage/analytics-server.Dockerfile
    container_name: "analytics-server-${USER}"
    depends_on:
      - clickhouse
    environment:
      COLUMN_STORAGE_CONFIG_PATH: /app/backend/column-storage/settings.yaml
    ports:
      - "9012:9012"
    networks:
      - avsp-net

  storage-server:
    build:
      context: ../..
      dockerfile: docker/storage/storage-server.Dockerfile
    container_name: "storage-server-${USER}"
    depends_on:
      - postgres
      - minio
      - minio-init
    environment:
      STORAGE_CONFIG_PATH: /app/storage/config/storage.yaml
    ports:
      - "9013:9012"
    networks:
      - avsp-net

  avsp-server:
    build:
      context: ../..
      dockerfile: docker/server/server.Dockerfile
    image: avsp-server
    container_name: "avsp-server-${USER}"
    depends_on:
      - postgres
      - minio
      - minio-init
      - embedder
      - vlm
      - storage-server
      - analytics-server
    volumes:
      - "${PWD}/../..:/app"
      - "vscode-server:/root/.vscode-server"
    ports:
      - "9002:9002"
    environment:
      S3_ENDPOINT_URL: http://minio:9000
      S3_ACCESS_KEY_ID: minioadmin
      S3_SECRET_ACCESS_KEY: minioadmin
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: avsp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      EMBEDDER_ENDPOINT: http://embedder:8000
      VLM_ENDPOINT: http://vlm:8001
      ANALYTICS_SERVER_ENDPOINT: http://analytics-server:9012
      ANALYTICS_SERVER_TIMEOUT_SEC: 30
      STORAGE_SERVER_ENDPOINT: http://storage-server:9012
      STORAGE_SERVER_TIMEOUT_SEC: 30
      STORAGE_WRITE_TOKEN: change-me-storage-write-token
    networks:
      - avsp-net

  frontend:
    image: node:20
    container_name: "frontend-${USER}"
    depends_on:
      - avsp-server
    working_dir: /app/frontend
    command: /bin/sh -c "npm ci --no-audit --no-fund && npm run dev -- --hostname 0.0.0.0 --webpack"
    volumes:
      - "${PWD}/../../frontend:/app/frontend"
      - "frontend-node-modules:/app/frontend/node_modules"
    ports:
      - "3000:3000"
    environment:
      MASTER_ENDPOINT: http://avsp-server:9002
      MINIO_PUBLIC_ENDPOINT: http://localhost:9000
      MINIO_BUCKET: avsp
      NODE_OPTIONS: --max-old-space-size=1024
    networks:
      - avsp-net

volumes:
  clickhouse-data:
  frontend-node-modules:
  minio-data:
  object-kv-data:
  postgres-data:
  vscode-server:

networks:
  avsp-net:
    driver: bridge
